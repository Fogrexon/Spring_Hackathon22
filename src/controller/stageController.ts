const stageData = {
  current: 0 as number,
  maps: [
    mapData1,
    mapData2,
    mapData3,
  ] as MapData[]
}

export const moveNextMap = () => {
  // 次のマップに移動
  // 最後のマップの場合はresult画面に移動
}

export const getCurrentMap = () => {
  // 現在のマップを取得
}

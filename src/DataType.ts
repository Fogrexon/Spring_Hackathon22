export interface MapData {
  width: number;
  height: number;
  data: string;
}

export interface PlayerData {
  x: number;
  y: number;
}

export interface GhostData {
  ghostx: number;
  ghosty: number;
  ghostspeed: number;
}